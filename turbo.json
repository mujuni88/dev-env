{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "install": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": []
    },
    "build": {
      "cache": false,
      "outputs": ["dist/**"]
    }
  }
}
