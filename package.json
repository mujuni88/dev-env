{
  "name": "dev-env",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "sync": "turbo sync",
    "setup": "bun install && bun run sync"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "trustedDependencies": [
    "turbo"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.0.25"
}
